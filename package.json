{
  "name": "html-examples",
  "version": "0.1.0",
  "private": true,
  "description": "Working HTML samples with measurements: reproduce the symptom, fix it, and compare implementations across browser engines.",
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": "^22.22.0 || >=24.8.0"
  },
  "scripts": {
    "measure:validation": "node tools/measure-validation.mjs",
    "check:html": "html-validate --config tools/.htmlvalidate.json symptoms compare"
  },
  "devDependencies": {
    "axe-core": "^4.12.1",
    "html-validate": "^11.6.0",
    "lighthouse": "^13.4.1",
    "playwright": "^1.56.0"
  }
}
